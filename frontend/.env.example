# Frontend Environment Variables
# This file is optional - the frontend has a proxy set in package.json

# Backend API URL (optional, defaults to proxy in package.json)
# REACT_APP_API_URL=http://localhost:5000
